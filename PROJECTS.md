# רשימת פרויקטים - קיטי טיול

מסמך זה מרכז את כל קיטי הטיול (תיקיות הלקוחות) בריפו, לפי סדר יצירה.

**הרשימה המלאה של הקיטים - כולל סטטוס פעיל/פג-תוקף בזמן אמת, קישורים וכפתורי שיתוף - עברה ל-`/projects.html`** (לוח בקרה מוגן בסיסמה, לא מקושר משום מקום ציבורי). קובץ זה (PROJECTS.md) ממשיך לתעד רק את תהליך העבודה וההחלטות הטכניות של הפרויקט.

רשימת קודי התיקיות לפי סדר יצירה (לצורך הפניה מהירה בזמן פיתוח - לפרטים מלאים ראו `projects.html`):

1. `kit_D422BRQ4djGiofHm7B3UG0ZvP` - שמואל ושרה אדרעי (תאילנד 2025)
2. `kit_VB55MpT3JwExUNvzk5kXeDX7E` - יותם דוד ויהודה בלס (תאילנד 2026)
3. `kit_Cud5p9lStXETlMnyiOjCT52CL` - יותם דוד ויהודה בלס, עותק של #2 (תאילנד 2026)
4. `kit_YSBn978vJhsDAKNqjbx4uPvDs` - שלומי יצחק דוד (אוסטריה 2026)
5. `kit_TUGPICKWf4ncBLzBIKnfOg4Nf` - אוראן הודיה ויהונתן נחום (מונטנגרו 2026)
6. `kit_mBsqjUDsfcsTS3cdJSMyWNaA5` - משפחת גברילוב (יוון-אתונה 2026)
7. `kit_ziXAOt2AEZDk33q4WBcldode1` - יהודית מלכה וינר ומוריה סופיר (בודפשט 2026)
8. `kit_K6cMERPmKLO01BFilPtgB7TjA` - תמר ושרה לוי לדקני (בודפשט 2026)
9. `kit_koXSCyp8vuU1HlNpra2bbCys2` - משפחת שלו (יוון-חלקידה 2026)

## תהליך עבודה

- כל תיקייה היא קיט טיול עצמאי (`Index.html`, `bkg.jpg`, `logo.jpg`, `files/`, `scripts/`) לפי אותה תבנית עיצובית.
- **מעכשיו והלאה**, בעת יצירת קיט חדש - יוצרים אותו כעותק של **התיקייה האחרונה שנוצרה** (הרשומה האחרונה ברשימה למעלה), אלא אם צוין אחרת במפורש.
- קוד התיקייה: `kit_` + מחרוזת אקראית בת 25 תווים (אותיות ומספרים בלבד). התחילית `kit_` היא חובה - היא מה שמאפשר גישה לתיקייה דרך אתר ה-Netyfy המאוחד (ראו סעיף הבא).
- **בכל יצירת פרויקט חדש יש לעדכן שני מקומות**: (1) להוסיף שורה לרשימת קודי התיקיות למעלה; (2) להוסיף אובייקט חדש למערך `KITS` בתוך `projects.html` (כולל `code`, `title` בפורמט "לקוח ביעד שנה", `country`, ו-`expiry` באותו תאריך של `KIT_EXPIRY` שהוגדר בקיט - ראו "מנגנון תפוגה" למטה). לוח הבקרה מחשב סטטוס פעיל/פג-תוקף בזמן ריצה מתוך `expiry`, ולא כערך שטוח.

## אחסון (Netlify)

- פרויקטים 1-8 פורסמו במקור כל אחד כאתר Netlify נפרד - אלה נשארים כפי שהם, בלי שינוי.
- **מעכשיו והלאה**: אתר Netlify אחד מחובר לריפו הזה (ענף `main`), עם `netlify.toml` בשורש. כל תיקיית קיט חדשה נגישה אוטומטית ברגע שהיא נדחפת ל-main - אין צורך ביצירת פרויקט Netlify חדש בכל פעם.
- **בקרת גישה**: `netlify.toml` מגדיר רשימת היתר (allow-list) - רק נתיבים שמתחילים ב-`/kit_` וכן `/thank-you.html`, `/404.html`, `/logo.jpg`, `/projects.html` נגישים. כל בקשה אחרת (כולל לקבצים אמיתיים כמו `PROJECTS.md` או תיקיית `app/`) מנותבת בכוח (`force = true`) לעמוד `404.html`.
- שם האתר המאוחד: `ho-travel.netlify.app`. קישור לדוגמה: `https://ho-travel.netlify.app/kit_<קוד>/Index.html`
- חשוב: יש לשמור על ה-`I` הגדולה במילה `Index.html` בקישורים שנשלחים ללקוחות (השרת רגיש לרישיות).

## מנגנון תפוגה אוטומטית

- לכל קיט יש קבוע `KIT_EXPIRY` בתוך תגית ה-`<script>` (בתחילת ה-`DOMContentLoaded` handler), המחושב כ: **יומיים אחרי התאריך המאוחר מבין** נחיתת טיסת החזרה ותאריך היציאה (checkout) האחרון מבין המלונות בנסיעה.
- מיד לאחר מסך הטעינה (באותה נקודה שבה בדרך כלל נחשף התוכן), הסקריפט בודק אם `new Date() >= KIT_EXPIRY`. אם כן - מפנה (`window.location.replace`) לעמוד `../thank-you.html` במקום לחשוף את התוכן.
- עמודי `404.html` ו-`thank-you.html` משותפים לכל הקיטים ויושבים בשורש הריפו (לא בתוך תיקיית קיט ספציפית).
- **חובה בכל קיט חדש**: להוסיף את `const KIT_EXPIRY = new Date('YYYY-MM-DDT00:00:00+03:00');` ואת בדיקת התפוגה לפני חשיפת התוכן, באותו מקום שבו זה מוטמע בקיטים הקיימים.

## נגישות (ת"י 5568 / WCAG 2.1 AA)

- לכל קובץ HTML בפרויקט (כל `Index.html`, `accessibility-statement.html`, `404.html`, `thank-you.html`) מוטמע widget נגישות עצמאי (inline, ללא תלות בקובץ חיצוני) עם כפתור צף (אייקון נגישות בתבנית SVG) ותפריט הכולל: הגדלת/הקטנת טקסט, ניגודיות גבוהה, הדגשת קישורים, עצירת אנימציות, ואיפוס - עם תמיכה מלאה במקלדת ותגיות ARIA.
- **חובה בכל קיט חדש**: להעתיק את בלוקי ה-`<style id="a11y-styles">`, כפתור/פאנל הנגישות והסקריפט הנלווה (מה-`Index.html` הקיים), וכן להעתיק את `accessibility-statement.html` לתוך תיקיית הקיט החדשה.
- בכל תיקיית קיט קיים קובץ `accessibility-statement.html` נפרד (זהה בתוכנו בין הקיטים) - כך שהוא נגיש דרך `/kit_<קוד>/accessibility-statement.html` בלי צורך בשינוי `netlify.toml`. בתחתית כל `Index.html` יש קישור יחסי (`accessibility-statement.html`) לעותק שבאותה תיקייה.
- ל-`404.html` ו-`thank-you.html` (שיושבים בשורש, מחוץ לתיקיות הקיטים) אין עותק ייעודי משלהם - הקישור שלהם להצהרת הנגישות מפנה לעותק הקנוני שבתיקיית הקיט האחרון (`kit_K6cMERPmKLO01BFilPtgB7TjA/accessibility-statement.html`). אם תיקייה זו יימחק בעתיד יש לעדכן את הקישור בשני הקבצים.

## לוח בקרה (`projects.html`)

- דף יחיד בשורש הריפו, מוגן במסך כניסה בסגנון iOS (הזנת קוד בן 4 ספרות בלוח מספרים) עם הסיסמה `5555`. זוהי הגנה צד-לקוח בלבד (הקוד גלוי בקוד המקור) - לא אמצעי אבטחה אמיתי, ולכן הדף לא מקושר משום מקום ציבורי (לא מה-404/thank-you/קיטים).
- לאחר הזנת קוד נכון, מוצגת טבלה עם כל הקיטים: כותרת, מדינה, סטטוס (מחושב ב-JS בזמן ריצה מתוך שדה `expiry` - לא ערך שטוח), כפתור שיתוף (שתי אפשרויות: עם טקסט נלווה / קישור בלבד - עם `navigator.share` ונפילה חזרה להעתקה ללוח), וכפתור פתיחה בטאב חדש.
- מקור הנתונים: מערך `KITS` בתוך הסקריפט של `projects.html` עצמו (אין קובץ JSON נפרד ואין נתיב API נוסף שצריך לחשוף ב-`netlify.toml`).
- **חובה בכל קיט חדש**: להוסיף אובייקט חדש למערך `KITS` (ראו סעיף "תהליך עבודה" למעלה).
