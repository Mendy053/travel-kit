# travel-kit - הנחיות עבודה

ריפו של H.O Travel (הודיה אוריאן) - קיטי טיול ללקוחות, ולוח בקרה פנימי.

רשימת הקיטים עצמם (כולל קישורים חיים וסטטוס) נמצאת ב-`/projects.html` (מוגן בסיסמה) - לא בקובץ הזה.

## יצירת קיט חדש

- כל תיקייה היא קיט טיול עצמאי (`Index.html`, `bkg.jpg`, `logo.jpg`, `files/`, `scripts/`) לפי אותה תבנית עיצובית.
- קיט חדש נוצר כעותק של **התיקייה האחרונה שנוצרה** (הרשומה האחרונה במערך `KITS` ב-`projects.html`), אלא אם צוין אחרת במפורש.
- קוד התיקייה: `kit_` + מחרוזת אקראית בת 25 תווים (אותיות ומספרים בלבד). התחילית `kit_` חובה - היא מה שמאפשר גישה דרך `netlify.toml`.
- **ברירת המחדל: להשתמש רק במידע שהלקוח מספק בפועל.** אין לייבא מידע מקיטים אחרים (למשל המלצות כשרות) ואין לחקור באינטרנט ולהוסיף תוכן עצמאי (אטרקציות, טיפים וכו') - **אלא אם התבקש לכך במפורש** באותה הודעה.
- **בכל יצירת קיט חדש יש להוסיף אובייקט חדש למערך `KITS` בתוך `projects.html`**: `code`, `title` בפורמט "לקוח ביעד שנה", `country`, ו-`expiry` (אותו תאריך של `KIT_EXPIRY` שהוגדר בקיט עצמו). הסטטוס פעיל/פג-תוקף מחושב בזמן ריצה מתוך `expiry`, לא כערך שטוח.

## אחסון (Netlify)

- אתר Netlify אחד (`ho-travel.netlify.app`) מחובר לריפו (ענף `main`), עם `netlify.toml` בשורש. כל תיקיית קיט חדשה נגישה אוטומטית ברגע שהיא נדחפת ל-main.
- **בקרת גישה**: `netlify.toml` מגדיר רשימת היתר (allow-list) - רק `/kit_*`, `/thank-you.html`, `/404.html`, `/logo.jpg`, `/projects.html` נגישים. כל בקשה אחרת מנותבת בכוח (`force = true`) ל-`404.html`.
- קישור לדוגמה: `https://ho-travel.netlify.app/kit_<קוד>/Index.html`. חשוב לשמור על ה-`I` הגדולה ב-`Index.html` (השרת רגיש לרישיות).

## מנגנון תפוגה אוטומטית

- לכל קיט יש קבוע `KIT_EXPIRY` בתוך תגית ה-`<script>` (בתחילת ה-`DOMContentLoaded` handler), המחושב כ: **יומיים אחרי התאריך המאוחר מבין** נחיתת טיסת החזרה ותאריך היציאה (checkout) האחרון מבין המלונות בנסיעה.
- מיד לאחר מסך הטעינה, הסקריפט בודק אם `new Date() >= KIT_EXPIRY`. אם כן - מפנה (`window.location.replace`) לעמוד `../thank-you.html` במקום לחשוף את התוכן.
- עמודי `404.html` ו-`thank-you.html` משותפים לכל הקיטים ויושבים בשורש הריפו.
- **חובה בכל קיט חדש**: להוסיף `const KIT_EXPIRY = new Date('YYYY-MM-DDT00:00:00+03:00');` ואת בדיקת התפוגה לפני חשיפת התוכן, באותו מקום שבו זה מוטמע בקיטים הקיימים.

## נגישות (ת"י 5568 / WCAG 2.1 AA)

- לכל קובץ HTML בפרויקט מוטמע widget נגישות עצמאי (inline, ללא תלות בקובץ חיצוני) עם כפתור צף (אייקון SVG) ותפריט: הגדלת/הקטנת טקסט, ניגודיות גבוהה, הדגשת קישורים, עצירת אנימציות, ואיפוס - עם תמיכה מלאה במקלדת ותגיות ARIA.
- **חובה בכל קיט חדש**: להעתיק את בלוקי ה-`<style id="a11y-styles">`, כפתור/פאנל הנגישות והסקריפט הנלווה, וכן להעתיק את `accessibility-statement.html` לתוך תיקיית הקיט החדשה.
- ל-`404.html` ו-`thank-you.html` (בשורש) אין עותק ייעודי - הקישור שלהם להצהרת הנגישות מפנה לעותק הקנוני שבתיקיית הקיט האחרון (`kit_K6cMERPmKLO01BFilPtgB7TjA/accessibility-statement.html`). אם תיקייה זו תימחק יש לעדכן את הקישור בשני הקבצים.

## העתקה ללוח (copy-to-clipboard)

- שדות חשובים (PNR, מספרי טיסה, כתובת/טלפון/אימייל של מלון) עטופים ב-`<span class="copyable" role="button" tabindex="0" aria-label="...">` עם אייקון SVG קטן - לחיצה/הקשה מעתיקה את הטקסט ללוח ומציגה טוסט אישור. עובד גם במקלדת (Enter/Space).
- התשתית (סגנון `.copyable`/`#copyToast` + הסקריפט) מוטמעת בכל `Index.html` בדיוק כמו בלוק הנגישות - מיד אחרי "End Accessibility Toolbar".
- **חובה בכל קיט חדש**: להעתיק את התשתית, ולעטוף PNR/מספרי טיסה/כתובת ומספרי טלפון/אימייל של המלון ב-`copyable`. אין לעטוף טקסט שכבר קישור (`<a>`, כמו קישורי מפות/WhatsApp) ואין לעטוף כתובות המוטמעות באמצע משפט תיאורי - רק שדות עצמאיים וברורים.
- **סימון טקסט (selection)**: `body` מוגדר עם `user-select: none` כברירת מחדל - טקסט תיאורי (המלצות, תוכן כללי) אינו ניתן לסימון/העתקה ידנית. חריגים שכן ניתנים לסימון: כותרות (`h1`-`h4`), שמות אתרים/מקומות המודגשים בתוך משפט תיאורי (`<strong>`, למשל "לגונת באלוס (Balos)"), שמות/תוויות מודגשים אחרים (`.font-bold`, `.font-semibold` - שמות מלונות/מסעדות, כותרות סעיפים), ושדות `.copyable`. **חובה בכל קיט חדש**: להעתיק את כללי ה-CSS האלה (מיד בתוך `body {}` ואחריו, בתחילת בלוק ה-`<style>` הראשי).

## לוח בקרה (`projects.html`)

- דף יחיד בשורש הריפו, מוגן במסך כניסה בסגנון iOS (קוד בן 4 ספרות, `5555`). הגנה צד-לקוח בלבד (הקוד גלוי בקוד המקור) - לא אמצעי אבטחה אמיתי, ולכן הדף לא מקושר משום מקום ציבורי.
- מציג טבלת קיטים: כותרת, מדינה, סטטוס (מחושב ב-JS מתוך `expiry`), כפתור שיתוף (עם טקסט נלווה / קישור בלבד, עם `navigator.share` ונפילה חזרה להעתקה ללוח), וכפתור פתיחה.
- מקור הנתונים: מערך `KITS` בתוך הסקריפט של `projects.html` עצמו - אין קובץ JSON נפרד.

## Git

- לפתח על ענף `claude/customer-data-swap-67wsp4`, לדחוף אליו, ולבקש אישור מפורש ("כן") לפני מיזוג ל-`main`. לא לדחוף ישירות ל-`main`.
